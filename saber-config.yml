siteConfig:
  title: Latex Diagrams
build:
  # / for surge
  # / dli-vue-docs 
  publicUrl: /
plugins:
  - resolve: saber-plugin-prismjs
  - resolve: saber-plugin-search
  - resolve: saber-plugin-copy-code
    options: 
      statusAttribute: title
      buttonStyle: 
        background-color: red
  - resolve: saber-plugin-google-analytics
    options:
      trackId: UA-119155027-4 # Google Analytics Track ID